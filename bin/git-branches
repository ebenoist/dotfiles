#!/usr/bin/env bash
# git-branches - remote branches, oldest commit first, with relative age
git branch -r --format='%(refname:short)' | while read -r branch; do
    printf '%s\t%s\n' \
        "$(git log -1 --pretty=format:"%Cgreen%ci %Cblue%cr%Creset" "$branch")" \
        "$branch"
done | sort
